Проверка разметки AsciiDoc.

Список:

* Элемент1
* Элемент2
* Элемент3
* Элемент4

Кусок кода:

[source,java]
----
while read a
do
  echo $a
done
----


[plantuml, hamburgers, png]
--
@startuml
start
:Вывалить мысли
в текст;
if (Бред?) then (Возможно)
    :Дать посмотреть коллеге;
    if (Сойдет?) then (Да)
    else (Нет)
        :Помедитировать над текстом;
    endif
    :Разместить на //Хабре//;
else (Точно)
    stop
endif
stop
@enduml

